= form_for @date do |f|
  - if @date.errors.any?
    #error_explanation
      %h2= "#{pluralize(@date.errors.count, "error")} prohibited this date from being saved:"
      %ul
        - @date.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
